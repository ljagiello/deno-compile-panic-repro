export = AuthOauth;
/**
 * Creates an oauth authenticator.
 *
 * @param {String} token
 *
 * @returns {Object}
 * @constructor
 */
declare function AuthOauth(token: string): Object;
declare class AuthOauth {
    /**
     * Creates an oauth authenticator.
     *
     * @param {String} token
     *
     * @returns {Object}
     * @constructor
     */
    constructor(token: string);
    /**
     * Update JSON body with token.
     *
     * @param {JSON} body
     *
     * @returns {null}
     */
    updateBody: (body: JSON) => null;
    authenticate: () => Promise<void>;
}
