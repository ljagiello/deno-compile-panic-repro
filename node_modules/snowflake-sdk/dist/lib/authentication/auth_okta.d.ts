export = AuthOkta;
/**
 * Creates an okta authenticator.
 *
 * @param {Object} connectionConfig
 * @param {HttpClient} httpClient
 *
 * @returns {Object}
 * @constructor
 */
declare function AuthOkta(connectionConfig: Object, httpClient: HttpClient): Object;
declare class AuthOkta {
    /**
     * Creates an okta authenticator.
     *
     * @param {Object} connectionConfig
     * @param {HttpClient} httpClient
     *
     * @returns {Object}
     * @constructor
     */
    constructor(connectionConfig: Object, httpClient: HttpClient);
    /**
     * Update JSON body with saml response.
     *
     * @param {JSON} body
     *
     * @returns {null}
     */
    updateBody: (body: JSON) => null;
    /**
     * Obtain saml response from Okta.
     *
     * @param {String} authenticator
     * @param {String} serviceName
     * @param {String} account
     * @param {String} username
     *
     * @returns {null}
     */
    authenticate: (authenticator: string, serviceName: string, account: string, username: string) => null;
    reauthenticate: (body: any, retryOption: any) => Promise<void>;
    /**
     *
     * @param {String} authenticator
     * @param {String} ssoUrl
     * @param {String} tokenUrl
     *
     * @returns {null}
     */
    validateURLs: (authenticator: string, ssoUrl: string, tokenUrl: string) => null;
}
