export = AuthWeb;
/**
 * Creates an external browser authenticator.
 *
 * @param {Object} connectionConfig
 * @param {Object} httpClient
 * @param {module} webbrowser
 *
 * @returns {Object}
 * @constructor
 */
declare function AuthWeb(connectionConfig: Object, httpClient: Object, webbrowser: any): Object;
declare class AuthWeb {
    /**
     * Creates an external browser authenticator.
     *
     * @param {Object} connectionConfig
     * @param {Object} httpClient
     * @param {module} webbrowser
     *
     * @returns {Object}
     * @constructor
     */
    constructor(connectionConfig: Object, httpClient: Object, webbrowser: any);
    /**
     * Update JSON body with token and proof_key.
     *
     * @param {JSON} body
     *
     * @returns {null}
     */
    updateBody: (body: JSON) => null;
    /**
     * Obtain SAML token through SSO URL.
     *
     * @param {String} authenticator
     * @param {String} serviceName
     * @param {String} account
     * @param {String} username
     *
     * @returns {Promise<null>}
     */
    authenticate: (authenticator: string, serviceName: string, account: string, username: string) => Promise<null>;
    generateProofKey: () => string;
    getLoginUrl: (username: any, proofKey: any, port: any) => string;
}
