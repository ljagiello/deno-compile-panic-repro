export function setIsRepresentNullAsStringNull(option: any): void;
export function getNullValue(): "NULL" | null;
export function toNativeType(sqlType: string): string;
declare namespace sqlTypes {
    namespace values {
        let TEXT: string;
        let BINARY: string;
        let BOOLEAN: string;
        let FIXED: string;
        let REAL: string;
        let DATE: string;
        let TIME: string;
        let TIMESTAMP_LTZ: string;
        let TIMESTAMP_NTZ: string;
        let TIMESTAMP_TZ: string;
        let VARIANT: string;
        let OBJECT: string;
        let ARRAY: string;
        let MAP: string;
    }
    function isString(sqlType: Object): boolean;
    function isBinary(sqlType: Object): boolean;
    function isBoolean(sqlType: Object): boolean;
    function isNumber(sqlType: Object): boolean;
    function isDate(sqlType: Object): boolean;
    function isTime(sqlType: Object): boolean;
    function isTimestamp(sqlType: Object): boolean;
    function isTimestampLtz(sqlType: Object): boolean;
    function isTimestampNtz(sqlType: Object): boolean;
    function isTimestampTz(sqlType: Object): boolean;
    function isVariant(sqlType: Object, fieldsMetadata: any): boolean;
    function isObject(sqlType: Object, fieldsMetadata: any): boolean;
    function isArray(sqlType: Object, fieldsMetadata: any): boolean;
    function isMap(sqlType: Object, fieldsMetadata: any): boolean;
}
declare namespace nativeTypes {
    export namespace values_1 {
        export let STRING: string;
        let BOOLEAN_1: string;
        export { BOOLEAN_1 as BOOLEAN };
        export let NUMBER: string;
        let DATE_1: string;
        export { DATE_1 as DATE };
        export let JSON: string;
        export let BUFFER: string;
        let OBJECT_1: string;
        export { OBJECT_1 as OBJECT };
        let ARRAY_1: string;
        export { ARRAY_1 as ARRAY };
        let MAP_1: string;
        export { MAP_1 as MAP };
    }
    export { values_1 as values };
    export function isValidValue(value: any): boolean;
    export function findInvalidValue(nativeTypes: string[]): number;
}
export { sqlTypes as SqlTypes, nativeTypes as NativeTypes };
