export namespace code {
    let INCORRECT_USERNAME_PASSWORD: string;
    let SESSION_TOKEN_INVALID: string;
    let GONE_SESSION: string;
    let SESSION_TOKEN_EXPIRED: string;
    let OAUTH_TOKEN_EXPIRED: string;
    let MASTER_TOKEN_EXPIRED: string;
    let ID_TOKEN_INVALID: string;
}
