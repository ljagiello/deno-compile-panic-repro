export = FileTransferAgent;
/**
 * Creates a file transfer agent.
 *
 * @param {Object} context
 *
 * @returns {Object}
 * @constructor
 */
declare function FileTransferAgent(context: Object): Object;
declare class FileTransferAgent {
    /**
     * Creates a file transfer agent.
     *
     * @param {Object} context
     *
     * @returns {Object}
     * @constructor
     */
    constructor(context: Object);
    /**
     * Execute PUT or GET command.
     *
     * @returns {null}
     */
    execute: () => null;
    /**
     * Generate the rowset and rowset types using the file metadatas.
     *
     * @returns {Object}
     */
    result: () => Object;
}
