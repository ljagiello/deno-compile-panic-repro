export = BrowserHttpClient;
/**
 * Creates a client that can be used to make requests in the browser.
 *
 * @param {ConnectionConfig} connectionConfig
 * @constructor
 */
declare function BrowserHttpClient(connectionConfig: ConnectionConfig): void;
declare class BrowserHttpClient {
    /**
     * Creates a client that can be used to make requests in the browser.
     *
     * @param {ConnectionConfig} connectionConfig
     * @constructor
     */
    constructor(connectionConfig: ConnectionConfig);
    /**
     * @inheritDoc
     */
    getRequestModule(): any;
}
